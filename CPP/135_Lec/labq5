#include <iostream>
#include <fstream>
using namespace std;
int main() {
	string line;
	string lines;
	string output;
	
	ifstream(outfile);
	outfile.open("data.txt");
	
	while (getline(outfile, lines)) {
	cout << lines << endl;
	}
return 0;
}
